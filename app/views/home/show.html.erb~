<% if !current_user.nil? %>
    <p>Click The link below to send a tweet.</p>
    <%= link_to "Send a Tweet", new_tweet_path %>

	<form class="class="navbar-search pull-left"">
		<%= form_tag(tweets_path, :method => "get", id: "search-form") do %>
		<%= text_field_tag :search, params[:search], placeholder: "Search" %>
		<%= submit_tag "Search" %>
		<% end %>
	</form>

	<div class="row-fluid">
		<% unless @tweets.nil? || @tweets.each do |f| %>
			<div class="span1 well">


					<p class="text-info"><%= raw auto_link(f.text) %>
					<span><%= raw time_ago_in_words(f.created_at) %> ago</span></p>

			</div>
		<% end %><% end %>
	</div>
	
<% else %>
    <p>You must log in to send a tweet.</p>
<% end %>
